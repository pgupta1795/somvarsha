const BlurBackground = () => (
  <div
    className="fixed inset-0 bg-slate-900/25 backdrop-blur transition-opacity opacity-100 bg-cover w-full"
    // style={{ height: document.body.scrollHeight }}
  />
);

export default BlurBackground;
